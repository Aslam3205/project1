// Entry point — delegates to the root server implementation.
// package.json points here (main: "src/server.js"), while the full
// application logic lives in server.js at the repository root.
require('../server.js');
